[project]
name = "events"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0",
    "python-ulid>=2.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"